<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="#">
    {{priu-icon name="logo"}} PRIU
  	</a>

    <div class="navbar-collapse">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          {{#link-to "index" class="nav-link"}}Visão Geral{{/link-to}}
        </li>
        <li class="nav-item">
          {{#link-to "story" "1" class="nav-link"}}Estória{{/link-to}}
        </li>
        <li class="nav-item ">
          <a class="nav-link" href="#">Instruções</a>
        </li>
      </ul>

      <div class="my-2 my-lg-0">
        {{#if userSession.isAuthenticated}}
          {{user-info user=model.user}}
          <button {{action "signOut"}}>Sign out</button>
        {{else}}
          <button {{action "signIn"}}>Entrar</button>
        {{/if}}
      </div>
    </div>
  </div>
</nav>

<div class="container pt-2">
	{{outlet}}
</div>